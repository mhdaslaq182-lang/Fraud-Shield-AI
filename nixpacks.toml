[phases.setup]
nixPkgs = ["cmake", "openblas", "liblapack", "python312"]

[phases.install]
cmds = ["pip install -r requirements.txt"]

[start]
cmd = "cd flask_app && gunicorn app:app --bind 0.0.0.0:$PORT --workers 1 --timeout 120"
